version: '2.1'
orbs:
  node-publisher: lbalmaceda/node-publisher@dev:9b37b36
workflows:
  build:
    jobs:
      - node-publisher/test
      - node-publisher/publish:
          context:
            - publish-npm
          requires:
            - node-publisher/test
          filters:
            branches:
              only:
                - master
